module.exports = {
    obj: {
        data: null
    },
    fn: ($) => {
        // nothing to do
    },
    _: {
        deps: {}, // no dependencies tracked
        stale: {}, // nothing to update
        value: { data: null }
    }
}