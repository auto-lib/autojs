module.exports = {
    obj: {
        // no values to wrap
    },
    fn: ($) => {
        // nothing to do
    },
    _: {
        deps: {},  // no dependencies tracked
        value: {}, // no values cached,
        stale: {}
    }
}